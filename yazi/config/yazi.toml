[opener]
edit = [
    {
        run = 'nvim %*',
        block = true,
        desc = "nvim",
        for = "unix"
    },
]

[[mgr.prepend_keymap]]
on   = "!"
for  = "unix"
run  = 'shell "$SHELL" --block'
desc = "Open $SHELL here"
